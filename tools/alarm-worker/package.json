{
  "name": "organizedchaos-alarms",
  "private": true,
  "type": "module",
  "description": "Holds a timebox appointment and sends the push when it comes due.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^1"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
